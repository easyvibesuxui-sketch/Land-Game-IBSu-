@AGENTS.md

## Deploying

Every push to `claude/devices-showroom-site-amc4re` publishes to GitHub Pages
via `.github/workflows/pages.yml`:

**https://easyvibesuxui-sketch.github.io/Land-Game-IBSu-/**

Report the deploy without being asked. After pushing a change, check the
workflow run and end the reply with the run's result and the link — the URL
never changes, so the only thing worth saying is whether the new build is live.
