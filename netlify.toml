[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  # Next 16 needs a current Node; pin it so the build does not drift.
  NODE_VERSION = "22"

[[plugins]]
  package = "@netlify/plugin-nextjs"
